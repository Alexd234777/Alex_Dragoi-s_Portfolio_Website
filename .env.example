NODE_ENV=production

# Web3Forms access keys are public by design. The browser uses this key
# directly because Render Free blocks SMTP and Web3Forms Free blocks server-side submissions.
WEB3FORMS_ACCESS_KEY=

# Optional only if you later move to paid hosting that allows SMTP.
GMAIL_USER=
GMAIL_APP_PASSWORD=
CONTACT_TO=
